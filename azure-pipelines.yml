trigger:
  - main
pool: 
 vmImage: 'ubuntu-latest'

stages:
  - stage: build
    displayName: login to acr
    jobs:
      - job: build
        steps:
          - task: Docker@2
            inputs:
              containerRegistry: 'acr-connection'
              repository: 'Docker'
              command: 'build'
              Dockerfile: 'Docker/docker-file/Dockerfile'
          - task: Docker@2
            inputs:
              containerRegistry: 'acr-connection'
              repository: 'Docker'
              command: 'push'
           